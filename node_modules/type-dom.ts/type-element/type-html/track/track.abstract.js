import { TypeHtml } from '../type-html.abstract';
export class TypeTrack extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('track');
        this.nodeName = 'track';
        this.dom = document.createElement(this.nodeName);
    }
}

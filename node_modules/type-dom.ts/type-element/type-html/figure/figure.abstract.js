import { TypeHtml } from '../type-html.abstract';
export class TypeFigure extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('figure');
        this.nodeName = 'figure';
        this.dom = document.createElement(this.nodeName);
    }
}

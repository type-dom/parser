import { TypeHtml } from '../type-html.abstract';
export class TypeDfn extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('dfn');
        this.nodeName = 'dfn';
        this.dom = document.createElement(this.nodeName);
    }
}

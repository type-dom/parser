import { TypeHtml } from '../type-html.abstract';
export class TypeAside extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('aside');
        this.nodeName = 'aside';
        this.dom = document.createElement(this.nodeName);
    }
}

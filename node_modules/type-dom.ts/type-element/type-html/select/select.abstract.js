import { TypeHtml } from '../type-html.abstract';
export class TypeSelect extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('select');
        this.nodeName = 'select';
        this.dom = document.createElement(this.nodeName);
    }
}

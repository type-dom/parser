import { TypeHtml } from '../type-html.abstract';
export class TypeMain extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('main');
        this.nodeName = 'main';
        this.dom = document.createElement(this.nodeName);
    }
}

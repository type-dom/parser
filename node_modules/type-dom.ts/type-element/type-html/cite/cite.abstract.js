import { TypeHtml } from '../type-html.abstract';
export class TypeCite extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('cite');
        this.nodeName = 'cite';
        this.dom = document.createElement(this.nodeName);
    }
}

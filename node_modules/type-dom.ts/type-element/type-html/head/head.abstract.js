import { TypeHtml } from '../type-html.abstract';
export class TypeHead extends TypeHtml {
    nodeName;
    dom;
    constructor(nodeName = 'h1') {
        super(nodeName);
        this.nodeName = nodeName;
        this.dom = document.createElement(this.nodeName);
    }
}

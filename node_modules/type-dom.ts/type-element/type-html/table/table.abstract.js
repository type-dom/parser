import { TypeHtml } from '../type-html.abstract';
export class TypeTable extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('table');
        this.nodeName = 'table';
        this.dom = document.createElement(this.nodeName);
    }
}

import { TypeHtml } from '../../type-html.abstract';
export class TypeTableCol extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('col');
        this.nodeName = 'col';
        this.dom = document.createElement(this.nodeName);
    }
}

import { TypeHtml } from '../../type-html.abstract';
export class TypeTableColGroup extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('colgroup');
        this.nodeName = 'colgroup';
        this.dom = document.createElement(this.nodeName);
    }
}

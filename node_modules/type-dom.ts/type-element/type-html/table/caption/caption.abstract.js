import { TypeHtml } from '../../type-html.abstract';
export class TypeTableCaption extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('caption');
        this.nodeName = 'caption';
        this.dom = document.createElement(this.nodeName);
    }
}

import { TypeHtml } from '../../type-html.abstract';
export class TypeTableHead extends TypeHtml {
    nodeName;
    dom;
    childNodes;
    constructor() {
        super('thead');
        this.nodeName = 'thead';
        this.dom = document.createElement(this.nodeName);
        this.childNodes = [];
    }
}

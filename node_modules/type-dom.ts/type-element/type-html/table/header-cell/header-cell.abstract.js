import { TypeHtml } from '../../type-html.abstract';
export class TypeTableHeaderCell extends TypeHtml {
    nodeName;
    dom;
    childNodes;
    constructor() {
        super('th');
        this.nodeName = 'th';
        this.dom = document.createElement(this.nodeName);
        this.childNodes = [];
    }
}

import { TypeHtml } from '../type-html.abstract';
export class TypeHeader extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('header');
        this.nodeName = 'header';
        this.dom = document.createElement(this.nodeName);
    }
}

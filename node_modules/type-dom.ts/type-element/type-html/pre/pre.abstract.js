import { TypeHtml } from '../type-html.abstract';
export class TypePre extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('pre');
        this.nodeName = 'pre';
        this.dom = document.createElement(this.nodeName);
    }
}

import { TypeHtml } from '../type-html.abstract';
export class TypeDialog extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('dialog');
        this.nodeName = 'dialog';
        this.dom = document.createElement(this.nodeName);
    }
}

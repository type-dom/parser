import { TypeHtml } from '../type-html.abstract';
export class TypeSamp extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('samp');
        this.nodeName = 'samp';
        this.dom = document.createElement(this.nodeName);
    }
}

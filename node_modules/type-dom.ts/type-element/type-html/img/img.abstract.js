import { TypeHtml } from '../type-html.abstract';
export class TypeImg extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('img');
        this.nodeName = 'img';
        this.dom = document.createElement(this.nodeName);
    }
}

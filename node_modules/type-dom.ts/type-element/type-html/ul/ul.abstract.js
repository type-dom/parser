import { TypeHtml } from '../type-html.abstract';
export class TypeUL extends TypeHtml {
    nodeName;
    dom;
    childNodes;
    constructor() {
        super('ul');
        this.nodeName = 'ul';
        this.dom = document.createElement(this.nodeName);
        this.childNodes = [];
    }
}

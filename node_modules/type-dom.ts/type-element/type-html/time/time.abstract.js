import { TypeHtml } from '../type-html.abstract';
export class TypeTime extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('time');
        this.nodeName = 'time';
        this.dom = document.createElement(this.nodeName);
    }
}

import { TypeHtml } from '../type-html.abstract';
export class TypeArea extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('area');
        this.nodeName = 'area';
        this.dom = document.createElement(this.nodeName);
    }
}

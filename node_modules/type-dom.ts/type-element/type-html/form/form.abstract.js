import { TypeHtml } from '../type-html.abstract';
export class TypeForm extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('form');
        this.nodeName = 'form';
        this.dom = document.createElement(this.nodeName);
    }
}

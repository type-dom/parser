import { TypeHtml } from '../type-html.abstract';
export class TypeDetails extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('details');
        this.nodeName = 'details';
        this.dom = document.createElement(this.nodeName);
    }
}

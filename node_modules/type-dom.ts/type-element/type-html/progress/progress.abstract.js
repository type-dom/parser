import { TypeHtml } from '../type-html.abstract';
export class TypeProgress extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('progress');
        this.nodeName = 'progress';
        this.dom = document.createElement(this.nodeName);
    }
}

import { TypeHtml } from '../type-html.abstract';
export class TypeArticle extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('article');
        this.nodeName = 'article';
        this.dom = document.createElement(this.nodeName);
    }
}

import { TypeHtml } from '../type-html.abstract';
export class TypeFieldset extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('fieldset');
        this.nodeName = 'fieldset';
        this.dom = document.createElement(this.nodeName);
    }
}

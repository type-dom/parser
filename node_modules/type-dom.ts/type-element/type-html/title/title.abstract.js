import { TypeHtml } from '../type-html.abstract';
export class TypeTitle extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('title');
        this.nodeName = 'title';
        this.dom = document.createElement(this.nodeName);
    }
}

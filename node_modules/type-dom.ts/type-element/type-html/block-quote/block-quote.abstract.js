import { TypeHtml } from '../type-html.abstract';
export class TypeBlockQuote extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('blockquote');
        this.nodeName = 'blockquote';
        this.dom = document.createElement(this.nodeName);
    }
}

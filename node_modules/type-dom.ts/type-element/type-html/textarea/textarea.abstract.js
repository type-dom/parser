import { TypeHtml } from '../type-html.abstract';
export class TypeTextarea extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('textarea');
        this.nodeName = 'textarea';
        this.dom = document.createElement(this.nodeName);
    }
}

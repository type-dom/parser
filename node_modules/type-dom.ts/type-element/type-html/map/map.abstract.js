import { TypeHtml } from '../type-html.abstract';
export class TypeMap extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('map');
        this.nodeName = 'map';
        this.dom = document.createElement(this.nodeName);
    }
}

import { TypeHtml } from '../type-html.abstract';
export class TypeAddress extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('address');
        this.nodeName = 'address';
        this.dom = document.createElement(this.nodeName);
    }
}

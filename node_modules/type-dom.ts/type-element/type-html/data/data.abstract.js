import { TypeHtml } from '../type-html.abstract';
export class TypeData extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('data');
        this.nodeName = 'data';
        this.dom = document.createElement(this.nodeName);
    }
}

import { TypeHtml } from '../type-html.abstract';
export class TypeSource extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('source');
        this.nodeName = 'source';
        this.dom = document.createElement(this.nodeName);
    }
}

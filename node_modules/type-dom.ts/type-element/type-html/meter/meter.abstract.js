import { TypeHtml } from '../type-html.abstract';
export class TypeMeter extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('meter');
        this.nodeName = 'meter';
        this.dom = document.createElement(this.nodeName);
    }
}

import { TypeHtml } from '../type-html.abstract';
export class TypeInput extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('input');
        this.nodeName = 'input';
        this.dom = document.createElement(this.nodeName);
    }
}

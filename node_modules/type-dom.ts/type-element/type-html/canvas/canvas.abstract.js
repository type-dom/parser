import { TypeHtml } from '../type-html.abstract';
export class TypeCanvas extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('canvas');
        this.nodeName = 'canvas';
        this.dom = document.createElement(this.nodeName);
    }
}

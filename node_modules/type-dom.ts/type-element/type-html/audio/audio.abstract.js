import { TypeHtml } from '../type-html.abstract';
export class TypeAudio extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('audio');
        this.nodeName = 'audio';
        this.dom = document.createElement(this.nodeName);
    }
}

import { TypeHtml } from '../type-html.abstract';
export class TypeMark extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('mark');
        this.nodeName = 'mark';
        this.dom = document.createElement(this.nodeName);
    }
}

import { TypeHtml } from '../type-html.abstract';
export class TypeVar extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('var');
        this.nodeName = 'var';
        this.dom = document.createElement(this.nodeName);
    }
}

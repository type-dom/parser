import { TypeHtml } from '../type-html.abstract';
export class TypeMenuItem extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('menuitem');
        this.nodeName = 'menuitem';
        this.dom = document.createElement(this.nodeName);
    }
}

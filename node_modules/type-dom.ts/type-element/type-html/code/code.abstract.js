import { TypeHtml } from '../type-html.abstract';
export class TypeCode extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('code');
        this.nodeName = 'code';
        this.dom = document.createElement(this.nodeName);
    }
}

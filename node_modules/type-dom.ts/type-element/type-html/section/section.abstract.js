import { TypeHtml } from '../type-html.abstract';
export class TypeSection extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('section');
        this.nodeName = 'section';
        this.dom = document.createElement(this.nodeName);
    }
}

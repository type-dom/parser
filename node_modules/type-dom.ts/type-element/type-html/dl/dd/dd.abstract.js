import { TypeHtml } from '../../type-html.abstract';
export class TypeDD extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('dd');
        this.nodeName = 'dd';
        this.dom = document.createElement(this.nodeName);
        this.childNodes = [];
    }
}

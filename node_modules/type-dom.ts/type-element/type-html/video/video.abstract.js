import { TypeHtml } from '../type-html.abstract';
export class TypeVideo extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('video');
        this.nodeName = 'video';
        this.dom = document.createElement(this.nodeName);
    }
}

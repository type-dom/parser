import { TypeHtml } from '../type-html.abstract';
export class TypeSummary extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('summary');
        this.nodeName = 'summary';
        this.dom = document.createElement(this.nodeName);
    }
}

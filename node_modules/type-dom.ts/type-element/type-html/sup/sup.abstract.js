import { TypeHtml } from '../type-html.abstract';
export class TypeSup extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('sup');
        this.nodeName = 'sup';
        this.dom = document.createElement(this.nodeName);
    }
}

import { TypeHtml } from '../type-html.abstract';
export class TypeOptGroup extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('optgroup');
        this.nodeName = 'optgroup';
        this.dom = document.createElement(this.nodeName);
    }
}

import { TypeHtml } from '../type-html.abstract';
export class TypeSpan extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('span');
        this.nodeName = 'span';
        this.dom = document.createElement(this.nodeName);
    }
}

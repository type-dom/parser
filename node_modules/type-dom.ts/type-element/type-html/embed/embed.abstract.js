import { TypeHtml } from '../type-html.abstract';
export class TypeEmbed extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('embed');
        this.nodeName = 'embed';
        this.dom = document.createElement(this.nodeName);
    }
}

import { TypeHtml } from '../type-html.abstract';
export class TypeLabel extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('label');
        this.nodeName = 'label';
        this.dom = document.createElement(this.nodeName);
    }
}

import { TypeHtml } from '../type-html.abstract';
export class TypeMenu extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('menu');
        this.nodeName = 'menu';
        this.dom = document.createElement(this.nodeName);
    }
}

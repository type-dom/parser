import { TypeHtml } from '../type-html.abstract';
export class TypeIFrame extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('iframe');
        this.nodeName = 'iframe';
        this.dom = document.createElement(this.nodeName);
    }
}

import { TypeHtml } from '../type-html.abstract';
export class TypeDataList extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('datalist');
        this.nodeName = 'datalist';
        this.dom = document.createElement(this.nodeName);
    }
}

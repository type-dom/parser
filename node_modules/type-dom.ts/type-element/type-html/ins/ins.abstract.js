import { TypeHtml } from '../type-html.abstract';
export class TypeIns extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('ins');
        this.nodeName = 'ins';
        this.dom = document.createElement(this.nodeName);
    }
}

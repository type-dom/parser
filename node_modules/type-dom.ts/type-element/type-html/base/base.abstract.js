import { TypeHtml } from '../type-html.abstract';
export class TypeBase extends TypeHtml {
    nodeName;
    dom;
    constructor() {
        super('base');
        this.nodeName = 'base';
        this.dom = document.createElement(this.nodeName);
    }
}

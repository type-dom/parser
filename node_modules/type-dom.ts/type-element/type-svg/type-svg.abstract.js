import { TypeElement } from '../type-element.abstract';
export class TypeSvg extends TypeElement {
    childNodes;
    constructor(nodeName) {
        super(nodeName);
        this.childNodes = [];
    }
}

import { TypeHtml } from '../type-html/type-html.abstract';
import { IComponent, ITypeComponent } from './type-component.interface';
import { TypeNode } from '../../type-node/type-node.abstract';
export declare abstract class TypeComponent extends TypeHtml implements ITypeComponent {
    abstract parent: TypeHtml;
    abstract setConfig(config: any): void;
    protected constructor(nodeName: string);
    createItem(parent: TypeHtml, node: IComponent): TypeComponent;
    createItems(parent: TypeHtml, nodes: IComponent[]): TypeNode[];
}

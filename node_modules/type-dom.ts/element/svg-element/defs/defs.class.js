import { TypeSvg } from "../../../type-element/type-svg/type-svg.abstract";
export class SvgDefs extends TypeSvg {
    parent;
    nodeName;
    className;
    dom;
    childNodes;
    constructor(parent) {
        super('defs');
        this.parent = parent;
        this.nodeName = 'defs';
        this.className = 'SvgDefs';
        this.dom = document.createElementNS('http://www.w3.org/2000/svg', this.nodeName);
        this.childNodes = [];
    }
}

import { TypeSvg } from '../../../type-element/type-svg/type-svg.abstract';
export class SvgRect extends TypeSvg {
    parent;
    nodeName;
    className;
    dom;
    propObj;
    childNodes;
    width = 60;
    height = 60;
    constructor(parent) {
        super('rect');
        this.parent = parent;
        this.nodeName = 'rect';
        this.className = 'SvgRect';
        this.dom = document.createElementNS('http://www.w3.org/2000/svg', this.nodeName);
        this.childNodes = [];
        this.propObj = {
            styleObj: {
                fill: 'rgb(255, 255, 255)',
                strokeWidth: 1,
                stroke: 'rgb(0, 0, 0)',
            },
            attrObj: {
                width: this.width,
                height: this.height
            }
        };
    }
    reset(width, height) {
        this.setAttrObj({
            width,
            height
        });
    }
}

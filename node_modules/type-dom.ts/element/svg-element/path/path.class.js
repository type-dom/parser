import { TypeSvg } from '../../../type-element/type-svg/type-svg.abstract';
export class SvgPath extends TypeSvg {
    parent;
    nodeName;
    dom;
    className;
    propObj;
    childNodes;
    constructor(parent) {
        super('path');
        this.parent = parent;
        this.nodeName = 'path';
        this.dom = document.createElementNS('http://www.w3.org/2000/svg', this.nodeName);
        this.className = 'SvgPath';
        this.propObj = {
            styleObj: {},
            attrObj: {
                d: ''
            }
        };
        this.childNodes = [];
    }
    setData(...rest) {
        this.addAttrObj({
            d: rest.join(' ')
        });
    }
    setFill(color) {
        this.addAttrObj({
            fill: color
        });
    }
}

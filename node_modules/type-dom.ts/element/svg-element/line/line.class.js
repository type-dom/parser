import { TypeSvg } from '../../../type-element/type-svg/type-svg.abstract';
export class SvgLine extends TypeSvg {
    parent;
    nodeName;
    className;
    dom;
    propObj;
    childNodes;
    x1 = 0;
    x2 = 0;
    y1 = 0;
    y2 = 0;
    constructor(parent) {
        super('line');
        this.parent = parent;
        this.nodeName = 'line';
        this.className = 'SvgLine';
        this.dom = document.createElementNS('http://www.w3.org/2000/svg', this.nodeName);
        this.childNodes = [];
        this.propObj = {
            styleObj: {
                strokeWidth: 1,
                stroke: 'rgb(0, 0, 0)',
            },
            attrObj: {
                x1: this.x1,
                y1: this.y1,
                x2: this.x2,
                y2: this.y2
            }
        };
    }
    reset(x1, y1, x2, y2) {
        this.setAttrObj({
            x1,
            y1,
            x2,
            y2
        });
        return this;
    }
}

import { TypeSvg } from "../../../type-element/type-svg/type-svg.abstract";
export class SvgLinearGradient extends TypeSvg {
    parent;
    nodeName;
    className;
    propObj;
    dom;
    childNodes;
    constructor(parent) {
        super('linearGradient');
        this.parent = parent;
        this.nodeName = 'linearGradient';
        this.className = 'SvgLinearGradient';
        this.dom = document.createElementNS('http://www.w3.org/2000/svg', this.nodeName);
        this.propObj = {
            styleObj: {},
            attrObj: {
                id: 'linear-1',
                x1: 0,
                y1: 0,
                x2: 0,
                y2: 0,
            }
        };
        this.childNodes = [];
        this.events = [];
    }
    reset(id) {
        this.setAttrObj({
            id,
        });
    }
}

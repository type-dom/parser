import { TypeSvg } from "../../../type-element/type-svg/type-svg.abstract";
export class SvgStop extends TypeSvg {
    parent;
    nodeName;
    className;
    dom;
    propObj;
    childNodes;
    constructor(parent) {
        super('stop');
        this.parent = parent;
        this.nodeName = 'stop';
        this.className = 'SvgStop';
        this.dom = document.createElementNS('http://www.w3.org/2000/svg', this.nodeName);
        this.childNodes = [];
        this.propObj = {
            styleObj: {},
            attrObj: {
                offset: '0%',
                stopColor: '#000',
            }
        };
        this.events = [];
    }
    reset(offset, stopColor) {
        this.setAttrObj({
            offset,
            stopColor,
        });
    }
}

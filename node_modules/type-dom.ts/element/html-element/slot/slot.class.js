import { TypeHtml } from '../../../type-element/type-html/type-html.abstract';
export class Slot extends TypeHtml {
    parent;
    nodeName;
    className;
    dom;
    constructor(parent) {
        super('slot');
        this.parent = parent;
        this.nodeName = 'slot';
        this.className = 'Slot';
        this.dom = document.createElement(this.nodeName);
    }
}

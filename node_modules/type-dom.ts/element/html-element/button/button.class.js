import { TypeButton } from '../../../type-element/type-html/button/button.abstract';
export class Button extends TypeButton {
    parent;
    className;
    constructor(parent) {
        super();
        this.parent = parent;
        this.className = 'Button';
    }
}

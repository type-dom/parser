import { TypeHtml } from '../../../type-element/type-html/type-html.abstract';
export class Template extends TypeHtml {
    parent;
    nodeName;
    className;
    dom;
    constructor(parent) {
        super('template');
        this.parent = parent;
        this.nodeName = 'template';
        this.className = 'Template';
        this.dom = document.createElement(this.nodeName);
    }
}

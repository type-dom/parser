import { TypeHtml } from '../../../type-element/type-html/type-html.abstract';
export class Header extends TypeHtml {
    parent;
    nodeName;
    dom;
    className;
    constructor(parent) {
        super('header');
        this.parent = parent;
        this.nodeName = 'header';
        this.dom = document.createElement(this.nodeName);
        this.className = 'Header';
    }
}

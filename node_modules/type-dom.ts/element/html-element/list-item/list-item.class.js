import { TypeHtml } from '../../../type-element/type-html/type-html.abstract';
export class ListItem extends TypeHtml {
    parent;
    nodeName;
    className;
    dom;
    childNodes;
    constructor(parent) {
        super('li');
        this.parent = parent;
        this.nodeName = 'li';
        this.dom = document.createElement(this.nodeName);
        this.className = 'ListItem';
        this.childNodes = [];
        this.propObj = {
            styleObj: {
                width: '100px',
                textAlign: 'center',
                padding: '6px 14px',
                borderRadius: '4px 4px 0px 0px',
                borderBottom: 'none',
                boxSizing: 'border-box'
            },
            attrObj: {
                name: 'list-item',
            },
        };
    }
}
